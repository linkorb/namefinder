{% extends "layout.html.twig" %}

{% block content %}
<h2>Rating: {{rating}}</h2>
<ul>
    {% for name in names %}
    <li><a href="/names/{{name}}">{{name}}</a></li>
    {% endfor %}
</ul>

{% endblock %}
